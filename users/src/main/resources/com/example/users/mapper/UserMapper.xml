<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.users.mapper.UserMapper">

    <!-- 根据邮箱登录 -->
    <select id="loginByEmail" resultType="com.example.users.pojo.User">
        select * from users where email = #{email}
    </select>
    <select id="loginByPhone" resultType="com.example.users.pojo.User">
        select * from users where phone = #{phone}
    </select>

    <!-- 插入 -->
    <insert id="insert" parameterType="com.example.users.pojo.User">
        insert into users (username, password, email, status, create_time)
        values (#{username}, #{password}, #{email}, #{status}, now())
    </insert>

    <!-- 更新状态 -->
    <update id="updateStatus">
        update users set status = #{status} where id = #{id}
    </update>

    <insert id="insertUser">
        insert into users(username, password, phone, email, status)
        values (#{username}, #{password}, #{phone}, #{email}, 1)
    </insert>

</mapper>